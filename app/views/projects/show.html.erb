<nav class="navbar navbar-expand-md navbar-dark-color fixed-top bg-dark">
  <div class="collapse navbar-collapse" id="navbarContent">
    <div class="navbar-nav mr-auto">
      <a class="nav-link" href="/">Home</a>
    </div>
      <% if user_signed_in? %>
          <a class="nav-link" href="/users/sign_out" data-method="delete" rel="nofollow">Logout</a>
      <% else %>
          <a class="nav-link" href="users/sign_in">Sign In/Sign Up</a>
      <% end %>
  </div>
</nav>

<br>
<br>
<br>

<div class="jumbotron"  style="text-align:center;">
  <div class="container">

<p id="notice"><%= notice %></p>

<p>
<h2><%= @project.title %></h2>
</p>

<p>
  <%= @project.description %>
</p>

<% if user_signed_in? %>
  <td><%= button_to 'Edit', edit_project_path(@project), method: :get, :class => "btn-primary btn-small" %></td>
<% end %>

<td><%= button_to 'Back', projects_path, method: :get, :class => "btn-primary btn-small" %></td>

  </div>
</div>


<br>

<footer class="container text-center">
  <b>Contact Information</b><p>Anthony Mitchell<br>719-375-5871<br>amitche3@uccs.edu</p>
</footer>